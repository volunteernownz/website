{
  "name": "volunteer_now",
  "version": "1.0.0",
  "description": "volunteer now platform",
  "author": "startup weekend group",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "golive": "git subtree push --prefix dist pages master"
  },
  "dependencies": {
    "@nuxtjs/vuetify": "0.5.5",
    "@nuxtjs/pwa": "^2.6.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2",
    "nodemon": "^1.18.9"
  }
}
